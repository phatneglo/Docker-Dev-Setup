# Loaded after the Bitnami/repmgr generated postgresql.conf.
# repmgr sets archive_command to /bin/true, so this file restores real WAL archiving.
archive_mode = on
archive_command = 'test ! -f /wal-archive/%f && cp %p /wal-archive/%f'
archive_timeout = '60s'
